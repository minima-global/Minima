package org.minima.tests.cli.help;

import org.junit.Test;
import static org.junit.Assert.assertNotNull;
import org.minima.system.commands.help;

import java.io.*;

public class HelpCommandTests {

    @Test
    public void testHelpNoArguments() {
/* 
        Process process = Runtime.getRuntime().exec("minima -jar");
        BufferedReader reader = new BufferedReader(new InputStreamReader(process.getOutputStream()));
        move = reader.readLine();
*/
    }

}